import { Typography, Box } from "@mui/material"
import { page } from '../styles/classes'

function Golfers() {
    return (
        <Box sx={page}>
            <Typography>This is going to be the Golfers page!</Typography>
        </Box>
    )
}

export default Golfers;