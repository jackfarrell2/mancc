import { Typography, Box } from "@mui/material"
import { page } from '../styles/classes'

function Vs() {
    return (
        <Box sx={page}>
            <Typography>This is going to be the VS page!</Typography>
        </Box>
    )
}

export default Vs;