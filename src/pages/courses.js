import { Typography, Box } from "@mui/material"
import { page } from '../styles/classes'

function Courses() {
    return (
        <Box sx={page}>
            <Typography>This is going to be the Courses page!</Typography>
        </Box>
    )
}

export default Courses;