import { Typography, Box } from "@mui/material"
import { page } from '../styles/classes'

function PostMatch() {
    return (
        <Box sx={page}>
            <Typography>This is going to be the Post a Match page!</Typography>
        </Box>
    )
}

export default PostMatch;